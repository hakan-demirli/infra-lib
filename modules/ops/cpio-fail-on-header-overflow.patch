diff --git a/src/copyout.c b/src/copyout.c
--- a/src/copyout.c
+++ b/src/copyout.c
@@ -285,14 +285,15 @@ to_ascii (char *where, uintmax_t v, size_t digits, unsigned logbase, bool nul)
 
 void
 field_width_error (const char *filename, const char *fieldname,
-		   uintmax_t value, size_t width, bool nul)
+		   uintmax_t value, size_t width, unsigned logbase, bool nul)
 {
   char valbuf[UINTMAX_STRSIZE_BOUND + 1];
   char maxbuf[UINTMAX_STRSIZE_BOUND + 1];
-  error (0, 0, _("%s: value %s %s out of allowed range 0..%s"),
+  error (PAXEXIT_FAILURE, 0,
+	 _("%s: value %s %s out of allowed range 0..%s"),
 	 quote (filename), fieldname,
 	 STRINGIFY_BIGINT (value, valbuf),
-	 STRINGIFY_BIGINT (MAX_VAL_WITH_DIGITS (width - nul, LG_8),
+	 STRINGIFY_BIGINT (MAX_VAL_WITH_DIGITS (width - nul, logbase),
 			   maxbuf));
 }
 
@@ -319,7 +320,7 @@ to_ascii_or_error (char *where, uintmax_t n, size_t digits,
 {
   if (to_ascii (where, n, digits, logbase, false))
     {
-      field_width_error (filename, fieldname, n, digits, false);
+      field_width_error (filename, fieldname, n, digits, logbase, false);
       return 1;
     }
   return 0;
diff --git a/src/extern.h b/src/extern.h
--- a/src/extern.h
+++ b/src/extern.h
@@ -124,7 +124,8 @@ void process_copy_out (void);
 int to_ascii (char *where, uintmax_t v, size_t digits, unsigned logbase,
 	      bool nul);
 void field_width_error (const char *filename, const char *fieldname,
-			uintmax_t value, size_t width, bool nul);
+			uintmax_t value, size_t width, unsigned logbase,
+			bool nul);
 
 /* copypass.c */
 void process_copy_pass (void);
diff --git a/src/tar.c b/src/tar.c
--- a/src/tar.c
+++ b/src/tar.c
@@ -86,7 +86,7 @@ to_oct_or_error (uintmax_t value, size_t digits, char *where, char const *field,
 {
   if (to_ascii (where, value, digits, LG_8, true))
     {
-      field_width_error (file, field, value, digits, true);
+      field_width_error (file, field, value, digits, LG_8, true);
       return 1;
     }
   return 0;
